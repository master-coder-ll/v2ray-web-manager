language: java
jdk:
  - openjdk8

addons:
  sonarcloud:
    organization: "master-coder-ll"
    token:
      secure: "$SONAR_TOKEN" # encrypted value of your token
script:
  - mvn  clean install  -Dmaven.test.skip=true  org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=master-coder-ll_v2ray-web-manager
#  - "mvn cobertura:cobertura"
#
after_success:
  - bash <(curl -s https://codecov.io/bash)
